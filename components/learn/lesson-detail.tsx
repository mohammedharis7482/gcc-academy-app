import MaterialCommunityIcons from '@expo/vector-icons/MaterialCommunityIcons';
import { Pressable, StyleSheet, View } from 'react-native';

import { AppButton } from '@/components/common/app-button';
import { AppText } from '@/components/common/app-text';
import { ProgressBar } from '@/components/common/progress-bar';
import { colors, layout, radius, shadows, spacing } from '@/design/tokens';
import { LearningLesson, LessonProgress } from '@/types/learning';
import { LessonThumbnail } from './lesson-thumbnail';

export function LessonVideoHero({ lesson, onBack, onPlay, ready, playing }: { lesson: LearningLesson; onBack: () => void; onPlay: () => void; ready: boolean; playing: boolean }) {
  return <View style={styles.hero}><LessonThumbnail thumbnail={lesson.thumbnail} style={styles.heroImage} /><View style={styles.shade} /><Pressable onPress={onBack} accessibilityRole="button" accessibilityLabel="Go back" style={({ pressed }) => [styles.back, pressed && styles.pressed]}><MaterialCommunityIcons name="arrow-left" size={22} color={colors.neutral.white} /></Pressable><Pressable onPress={onPlay} accessibilityRole="button" accessibilityLabel={`${playing ? 'Pause' : ready ? 'Continue' : 'Play'} ${lesson.title}`} style={({ pressed }) => [styles.play, pressed && styles.pressed]}><MaterialCommunityIcons name={playing ? 'pause' : 'play'} size={30} color={colors.neutral.white} /></Pressable><View style={styles.duration}><MaterialCommunityIcons name="clock-outline" size={14} color={colors.neutral.white} /><AppText variant="caption" weight="bold" color={colors.neutral.white}>{lesson.durationMinutes} min</AppText></View>{ready && <View style={styles.ready}><AppText variant="caption" weight="bold" color={colors.neutral.white}>{playing ? 'DEMO PLAYING' : 'READY TO CONTINUE'}</AppText></View>}</View>;
}

export function LessonMetadata({ lesson }: { lesson: LearningLesson }) {
  return <View style={styles.metadata}><AppText variant="caption" weight="extraBold" color={colors.brand.blue}>{lesson.category.toUpperCase()}</AppText><AppText variant="title" weight="extraBold">{lesson.title}</AppText><AppText variant="body" color={colors.neutral.textSecondary}>{lesson.description}</AppText><View style={styles.metaGrid}><Meta icon="account-outline" label={`Coach ${lesson.coachName}`} value={lesson.coachRole} /><Meta icon="account-group-outline" label="Suitable for" value={lesson.suitableCategories.join(' · ')} /><Meta icon="calendar-blank-outline" label="Published" value={lesson.publishedDate} /><Meta icon="clock-outline" label="Duration" value={`${lesson.durationMinutes} minutes`} /></View></View>;
}
function Meta({ icon, label, value }: { icon: keyof typeof MaterialCommunityIcons.glyphMap; label: string; value: string }) { return <View style={styles.meta}><View style={styles.metaIcon}><MaterialCommunityIcons name={icon} size={18} color={colors.brand.blue} /></View><View style={styles.metaCopy}><AppText variant="caption" color={colors.neutral.textSecondary}>{label}</AppText><AppText variant="bodySmall" weight="semibold" numberOfLines={2}>{value}</AppText></View></View>; }

export function LearningObjectivesCard({ objective }: { objective: string }) { return <InfoCard icon="target" title="Learning objective"><AppText variant="body" color={colors.neutral.textSecondary}>{objective}</AppText></InfoCard>; }
export function PracticePointsCard({ points }: { points: readonly string[] }) { return <InfoCard icon="clipboard-check-outline" title="Practice points"><View style={styles.points}>{points.map((point) => <View key={point} style={styles.point}><View style={styles.check}><MaterialCommunityIcons name="check" size={15} color={colors.brand.blue} /></View><AppText variant="bodySmall" weight="medium" style={styles.pointText}>{point}</AppText></View>)}</View></InfoCard>; }
function InfoCard({ icon, title, children }: { icon: keyof typeof MaterialCommunityIcons.glyphMap; title: string; children: React.ReactNode }) { return <View style={styles.card}><View style={styles.cardTitle}><View style={styles.titleIcon}><MaterialCommunityIcons name={icon} size={20} color={colors.brand.blue} /></View><AppText variant="heading" weight="extraBold">{title}</AppText></View>{children}</View>; }

export function LessonProgressCard({ lesson, progress, onContinue, onComplete }: { lesson: LearningLesson; progress: LessonProgress; onContinue: () => void; onComplete: () => void }) {
  const completed = progress.status === 'completed';
  return <View style={styles.card}><View style={styles.progressHeader}><View style={styles.progressCopy}><AppText variant="heading" weight="extraBold">Current progress</AppText><AppText variant="bodySmall" color={colors.neutral.textSecondary}>{completed ? 'Lesson completed' : `${progress.watchedMinutes} of ${lesson.durationMinutes} minutes watched`}</AppText></View><View style={[styles.status, completed && styles.statusComplete]}><AppText variant="caption" weight="bold" color={completed ? colors.status.success : colors.brand.blue}>{completed ? 'COMPLETED' : progress.status === 'not-started' ? 'NOT STARTED' : 'IN PROGRESS'}</AppText></View></View><ProgressBar progress={progress.watchedMinutes / lesson.durationMinutes} color={completed ? colors.status.success : colors.brand.blue} trackStyle={styles.track} accessibilityLabel={`${Math.round(progress.watchedMinutes / lesson.durationMinutes * 100)} percent of ${lesson.title} watched`} />{completed ? <AppButton label="Completed" disabled accessibilityLabel={`${lesson.title} completed`} /> : <View style={styles.actions}><AppButton label={progress.status === 'not-started' ? 'Start lesson' : 'Continue watching'} onPress={onContinue} accessibilityLabel={`${progress.status === 'not-started' ? 'Start' : 'Continue watching'} ${lesson.title}`} style={styles.primaryAction} /><AppButton label="Mark complete" variant="secondary" onPress={onComplete} accessibilityLabel={`Mark ${lesson.title} complete`} style={styles.secondaryAction} /></View>}</View>;
}
export function CoachNoteCard({ lesson }: { lesson: LearningLesson }) { return <View style={styles.note}><View style={styles.noteIcon}><MaterialCommunityIcons name="message-text-outline" size={22} color={colors.brand.blue} /></View><View style={styles.noteCopy}><AppText variant="caption" weight="extraBold" color={colors.brand.blue}>COACH {lesson.coachName.toUpperCase()}</AppText><AppText variant="body" weight="medium">{lesson.coachNote}</AppText></View></View>; }

const styles = StyleSheet.create({ hero: { aspectRatio: 16 / 9, borderRadius: radius.hero, overflow: 'hidden', backgroundColor: colors.neutral.border }, heroImage: { ...StyleSheet.absoluteFillObject, borderRadius: 0 }, shade: { ...StyleSheet.absoluteFillObject, backgroundColor: colors.mediaOverlay }, back: { position: 'absolute', top: spacing.sm, left: spacing.sm, width: layout.minTouchTarget, height: layout.minTouchTarget, borderRadius: radius.pill, backgroundColor: colors.mediaControl, alignItems: 'center', justifyContent: 'center' }, play: { position: 'absolute', top: '50%', left: '50%', marginLeft: -26, marginTop: -26, width: 52, height: 52, borderRadius: radius.pill, backgroundColor: colors.mediaControl, alignItems: 'center', justifyContent: 'center' }, duration: { position: 'absolute', right: spacing.sm, bottom: spacing.sm, minHeight: 30, paddingHorizontal: spacing.sm, borderRadius: radius.pill, backgroundColor: colors.mediaBadge, flexDirection: 'row', alignItems: 'center', gap: spacing.xs }, ready: { position: 'absolute', left: spacing.sm, bottom: spacing.sm, minHeight: 30, paddingHorizontal: spacing.sm, borderRadius: radius.pill, backgroundColor: colors.brand.blue, justifyContent: 'center' }, metadata: { gap: spacing.xs }, metaGrid: { marginTop: spacing.sm, flexDirection: 'row', flexWrap: 'wrap', gap: spacing.sm }, meta: { width: '48%', flexDirection: 'row', alignItems: 'center', gap: spacing.xs }, metaIcon: { width: 36, height: 36, borderRadius: radius.small, backgroundColor: colors.brand.blueSoft, alignItems: 'center', justifyContent: 'center' }, metaCopy: { flex: 1, minWidth: 0 }, card: { ...shadows.card, padding: layout.cardPadding, borderRadius: radius.large, borderWidth: 1, borderColor: colors.neutral.border, backgroundColor: colors.neutral.surface, gap: spacing.sm }, cardTitle: { flexDirection: 'row', alignItems: 'center', gap: spacing.sm }, titleIcon: { width: layout.rowIconSize, height: layout.rowIconSize, borderRadius: radius.small, backgroundColor: colors.brand.blueSoft, alignItems: 'center', justifyContent: 'center' }, points: { gap: spacing.sm }, point: { flexDirection: 'row', alignItems: 'center', gap: spacing.sm }, check: { width: 28, height: 28, borderRadius: radius.pill, backgroundColor: colors.brand.blueSoft, alignItems: 'center', justifyContent: 'center' }, pointText: { flex: 1 }, progressHeader: { flexDirection: 'row', alignItems: 'flex-start', justifyContent: 'space-between', gap: spacing.sm }, progressCopy: { flex: 1, minWidth: 0 }, status: { paddingVertical: spacing.xs, paddingHorizontal: spacing.sm, borderRadius: radius.pill, backgroundColor: colors.brand.blueSoft }, statusComplete: { backgroundColor: colors.status.successSoft }, track: { height: 8, borderRadius: radius.pill, backgroundColor: colors.neutral.divider, overflow: 'hidden' }, fill: { height: '100%', borderRadius: radius.pill, backgroundColor: colors.brand.blue }, fillComplete: { backgroundColor: colors.status.success }, actions: { flexDirection: 'row', gap: spacing.xs }, primaryAction: { flex: 1, paddingHorizontal: spacing.sm }, secondaryAction: { flex: 1, paddingHorizontal: spacing.sm }, note: { padding: layout.cardPadding, borderRadius: radius.large, borderWidth: 1, borderColor: colors.avatarBorder, backgroundColor: colors.brand.blueSoft, flexDirection: 'row', gap: spacing.sm }, noteIcon: { width: layout.rowIconSize, height: layout.rowIconSize, borderRadius: radius.medium, backgroundColor: colors.neutral.white, alignItems: 'center', justifyContent: 'center' }, noteCopy: { flex: 1, gap: spacing.xs }, pressed: { opacity: 0.8, transform: [{ scale: 0.98 }] } });
